const base = 10;
const altura = 5;

const area = base * altura;
const perimetro = 2 * (base + altura);

console.log(`Area del rectángulo: ${area} cm²`);
console.log(`Perímetro del rectángulo: ${perimetro} cm`);